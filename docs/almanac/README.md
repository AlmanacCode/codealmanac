# Supabase Agent Wiki Prototype

This demo wiki shows a broad Supabase agent coverage map. Most pages are stubs so the navigation feels like a real product wiki without pre-writing every article.

The polished seed articles are:

- [[building-supabase-with-agents]]
- [[row-level-security-in-supabase]]

The pages are organized by the coverage map in `coverage-map.md`. Full articles use local source citations in the form `[@source-id]` and wikilinks in the form `[[page-slug]]`.
