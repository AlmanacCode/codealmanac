---
page_id: reference
title: Reference
topics: [reference]
sources:
  - id: manual-page-selection
    type: file
    path: docs/almanac/_manual/page-selection.md
    note: Defines reference pages as exact public contracts rather than implementation summaries.
---

# Reference

`reference/` records exact contracts that readers need to look up: CLI commands,
flags, environment variables, config keys, file formats, schemas, public APIs,
and stable behavior.

Keep reference pages concrete. Do not duplicate implementation internals that
are better read from code.
